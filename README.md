## 1. Chạy dự án
  ```bash
  npm start
  npm run start:ssr
  npm run serve:ssr - (dùng node chạy khi đã build)
  ```
## 2. Build production
  ```bash
  ng build
  npm run build:ssr (build with universal)
  ```
## 3. Node cho dự án
  README_DOC.md
